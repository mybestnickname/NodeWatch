# Runtime + development dependencies for CI and local testing.
# The application itself is installed via pipenv (see Pipfile); this file lets
# CI and contributors set up everything with a single `pip install`.

# --- runtime (kept in sync with Pipfile) ---
anyio==4.3.0
exceptiongroup==1.2.0
fastapi==0.110.2
orjson==3.10.3
pydantic==2.7.1
uvicorn[standard]==0.29.0

# --- testing & quality ---
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.6
ruff==0.4.4
mypy==1.10.0
